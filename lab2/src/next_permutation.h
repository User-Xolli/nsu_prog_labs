#ifndef NEXT_PERMUTATION_H
#define NEXT_PERMUTATION_H
#include <stdbool.h>
#include <string.h>
#include "number_string.h"

bool next_permutation(char *str);
// true - permutation exist
// false - permutation don't exist

#endif
